<Window x:Class="StreamCapture.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="平台配置" Height="600" Width="800"
        Icon="favicon.ico"
        WindowStartupLocation="CenterScreen"
        Background="#0d1117"
        Foreground="#e6edf3">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#58a6ff"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#79b8ff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="平台配置管理" FontSize="20" FontWeight="Bold" Foreground="#e6edf3" Margin="0,0,0,15"/>

        <Border Grid.Row="1" Background="#161b22" CornerRadius="6" Padding="15"
                BorderBrush="#30363d" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="PlatformItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#30363d" BorderThickness="0,0,0,1" Padding="10" Margin="0,5">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding Enabled}" VerticalAlignment="Center" Foreground="#e6edf3"/>
                                        <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" Foreground="#e6edf3" Margin="10,0,0,0"/>
                                        <Border Background="#3fb950" CornerRadius="3" Padding="6,2" Margin="10,0,0,0">
                                            <TextBlock Text="{Binding Id}" Foreground="White" FontSize="11"/>
                                        </Border>
                                    </StackPanel>

                                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
                                        <TextBlock Text="协议: " FontSize="13" Foreground="#8b949e"/>
                                        <ItemsControl ItemsSource="{Binding Protocols}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="#58a6ff" CornerRadius="3" Padding="4,2" Margin="0,0,5,0">
                                                        <TextBlock Text="{Binding}" Foreground="White" FontSize="11"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5,0,0">
                                        <TextBlock Text="端口: " FontSize="13" Foreground="#8b949e"/>
                                        <ItemsControl ItemsSource="{Binding Ports}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding}" Margin="0,0,8,0" FontSize="13" Foreground="#c9d1d9"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,5,0,0">
                                        <TextBlock Text="域名匹配: " FontSize="13" Foreground="#8b949e"/>
                                        <TextBlock Text="{Binding DomainPatterns[0]}" FontSize="12" Foreground="#58a6ff"/>
                                        <TextBlock Text=" ..." FontSize="12" Foreground="#6e7681" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="保存配置" Style="{StaticResource ModernButton}" Click="SaveButton_Click" Width="120" Margin="0,0,10,0"/>
            <Button Content="取消" Style="{StaticResource ModernButton}" Click="CancelButton_Click" Width="120" Background="#21262d"/>
        </StackPanel>
    </Grid>
</Window>
